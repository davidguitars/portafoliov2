---
const { text, link, target = "_self", variant = "primary" } = Astro.props;
---
<a href={link} target={target} class={`btn-${variant}`}>
  {text}
  <span class="mono" aria-hidden="true">→</span>
</a>
